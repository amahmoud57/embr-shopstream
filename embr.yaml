platform: nodejs
platformVersion: "20"
autoDeploy: true

run:
  port: 3000
  startCommand: node server.js

database:
  enabled: true
  engine: postgres
  framework: raw
  schema: db/schema.sql
  seed: "psql \"$DATABASE_URL\" -f db/seed.sql"
